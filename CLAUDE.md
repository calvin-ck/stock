# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 입력 폼 규칙

모든 페이지의 입력 폼은 메인 조회 페이지(`/`)의 방식을 기준으로 한다:

- **종목 코드**: 메인 페이지는 자유 입력(신규 종목 조회용), 그 외 페이지는 로컬에 이미
  저장된 종목 목록에서 드롭다운으로 선택(`_csv_file_selector.html` 공유 partial).
- **종료일**(`end_date`): 비우면 오늘.
- **시작일**(`start_date`) 또는 **기간**(`period`, 일 단위) 중 하나: 시작일을 입력하면
  기간은 무시된다. 둘 다 비우면 기간 기본값 30일이 적용된다. 두 필드는 서로 반대쪽을
  비우는 스크립트가 붙어 있어(사용자가 마지막으로 건드린 쪽만 서버로 전달됨), 새 폼을
  만들 때도 이 스크립트를 그대로 재사용해야 한다.
- 종목 코드/종료일/기간에는 각각 기본값(102110/오늘/30일)이 실제 값으로 채워져서
  렌더링된다(힌트 텍스트가 아니라 입력값 자체).

기능에 따라 위 공통 필드에 더해 전략별 입력(gap%, 수량%, 옵션 체크박스 등)을 추가로
받을 수 있다.

## 메뉴/페이지 제목 규칙

전략별 백테스트/히트맵 한 쌍은 `_nav.html`에서 같은 `nav-group`으로 묶이고, 그룹 라벨이
전략 이름(제목) 역할을 한다:

- **메뉴**: 그룹 라벨에 전략 이름을 한 번만 적고, 그 아래 링크는 각각 "백테스트",
  "히트맵"으로만 쓴다(축 종류, gap/수량 같은 세부 설명을 링크 텍스트에 덧붙이지 않는다).
- **페이지 제목**(`<title>`, `<h1>`): "제목 백테스트", "제목 히트맵" 형태로 통일한다
  (예: "하락 매도 상승 매수(익절) 백테스트", "하락 매도 상승 매수(익절) 히트맵"). 히트맵 페이지
  제목에 " — gap × 수량"처럼 스윕 축을 덧붙이지 않는다. 축에 대한 설명은 제목이 아니라
  `<h1>` 아래 불릿 설명에 적는다.

## 페이지 상단 설명 규칙

각 페이지 상단(`<h1>` 아래)의 설명은 서술형 문단이 아니라 **불릿 목록**으로 작성한다.
순서는 대략 다음과 같되, 페이지 성격에 맞지 않는 항목은 생략해도 된다:

- 이 페이지가 무엇을 하는 페이지인지 한두 줄 개요
- 작동 방식(계산/매매 로직이 어떤 순서로 동작하는지)
- 각 기능(옵션, 체크박스, 링크 등)에 대한 설명
- 필요하면 예시

"작동 방식: ~~~", "기능: ~~~"처럼 항목 제목 뒤에 콜론을 붙여 딱딱하게 나열하지 않고,
자연스러운 소제목(하위 불릿을 담는 상위 항목)으로 녹여서 쓴다.

페이지 설명(이 페이지 상단 설명뿐 아니라 CLAUDE.md/README의 페이지별 설명도 포함)을 다시
정리할 때는 아래 기준을 따른다:

- 그 페이지가 **지금** 실제로 하는 일만 적는다. 다른 페이지와 비교하거나("`/backtest`와
  로직은 같지만 ~~~가 없다") 예전에 있었다가 뺀 기능을 근거로 설명하지 않는다 — 그 페이지
  자체의 기능만으로 독립적으로 설명한다.
- 여러 페이지에 공통으로 적용되는 사실(예: "로컬 CSV만 읽으므로 네이버에 다시 접속하지
  않는다")은 이미 README 등 공통 문서에 정리되어 있다면 페이지별 설명에서 반복하지 않는다.
- 정리 후에는 실제로 값을 넣어 페이지를 렌더링해보고(스크린샷 또는 curl) 화면에 남은 다른
  설명 문구·표·링크와 모순되지 않는지 확인한다.

## 페이지

- **`/`**: 종목 코드와 기간으로 네이버 금융에서 일별 시세를 가져와 로컬 캐시
  (`data/{code}.csv`)에 저장한다. 상승/하락/보합 일수 비율, 연속 상승·하락 일수 분포,
  등락률 분포, 당일갭(종가−시가)·전일갭(시가−전일종가) 추이 같은 가격 통계를 입력 폼
  바로 아래에 보여주고, 일별 시세 표/차트는 페이지 맨 아래에 보여준다.
- **`/grid_trade`**: 트레일링 고점(max)/저점(min)을 계속 추적하다가, 거기서 매매gap%만큼
  움직이면 매매하는 백테스트. 매도/매수 gap이 하나의 값으로 통일되어 있다.
- **`/grid_trade_heatmap`**: `/grid_trade`의 매매gap% × 매매수량% 조합별 수익률 히트맵.
- **`/profit_recovery`**: 그리드 매수/매도 없이, 자본금 대비 평가금액이 이익gap%만큼 벌면
  평가차익 중 회수율%를 현금(부족하면 주식 매도)으로 회수해 적립금으로 옮기는 백테스트.
- **`/profit_recovery_heatmap`**: `/profit_recovery`의 이익gap% × 회수율% 조합별 수익률 히트맵.
- **`/daily_reversal`**: 전날 종가보다 gap%만큼 오르면 매도, 내리면 매수하는 역추세
  백테스트. 고점/저점 추적은 없다. 매도/매수 수량은 시작 보유 주식수 대비 %로 하나의
  값을 공유한다.
- **`/daily_reversal_heatmap`**: `/daily_reversal`의 gap% × 매매수량% 조합별 수익률 히트맵.
- **`/daily_gap`**: 트레일링 고점에서 gap%만큼 내리면 매수, 저점에서 gap%만큼 오르면
  매도하는 역추세 백테스트. 매도/매수 수량은 하나의 값(시작 보유 주식수 대비 %)을
  공유한다.
- **`/daily_gap_heatmap`**: `/daily_gap`의 gap% × 매매수량% 조합별 수익률 히트맵.
- **`/daily_reference`**: 고정된 기준가 대비 gap% 이상 오르면 매도, 내리면 매수하는 역추세
  백테스트. 기준가는 매매가 일어날 때만 그 거래가로 갱신되며, 트레일링 고점/저점을
  따라가지 않는다.
- **`/daily_reference_heatmap`**: `/daily_reference`의 gap% × 매매수량% 조합별 수익률 히트맵.
- **`/capital_recovery`**: 주식평가금액을 유지하기 위한 전략. 기준가 × 시작 보유
  주식수로 정한 자본금을 기준으로, 주식 평가금액이 자본금과 매매gap% 이상 차이 날 때만
  매매해서 넘으면 초과분을, 내려가면 부족분을 매매율%만큼 매매하는 백테스트.
- **`/capital_recovery_heatmap`**: `/capital_recovery`의 매매gap% × 매매율% 조합별 수익률 히트맵.
- **`/best`**: 위 전략별 히트맵을 각 히트맵의 기본 스윕 범위로 한 번씩 계산해서, 그중
  최고 수익률 조합만 모아 전략끼리 비교한다.
- **`/best_heatmap`**: `/best`의 6개 전략 × 6개 기간(180/90/60/30/14/7일)의 최고 수익률을
  한 번에 비교하는 히트맵.
- **`/best_sweep_heatmap`**: 6개 전략 × 수량류 축(qty_pct·회수율·매매율) 11개 지점
  (1, 10, 20, ..., 100%)의 최고 수익률을 한 번에 비교하는 히트맵. 각 칸은 그 수량값을
  고정하고 gap류 축(gap·profit_gap·gap_pct)을 1~100% 전체로 스윕했을 때 나온 최고
  수익률이다.
